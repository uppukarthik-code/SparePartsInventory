# Railway Inventory Planning Platform v1.0 — RUNTIME dependencies (pinned)
# Purified in STEP33: scikit-learn / prophet / plotly REMOVED (verified 0 imports in railway/).
# Notebook/dev extras: see requirements-notebooks.txt and pyproject.toml [project.optional-dependencies].
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
statsmodels==0.14.6
pulp==3.3.2
